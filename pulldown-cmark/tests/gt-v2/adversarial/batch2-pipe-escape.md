# Batch-2: pipe metadata, escaped pipe, escaped bang

> [!note|meta] pipe metadata splits at the pipe
> body

> [!note|meta unclosed stays a plain quote
> second line

Escaped pipe [[Page\|esc-alias]] still splits the alias

Control [[Page|plain-alias]] with a plain pipe

Escaped bang \![[not-bound]] defeats embed binding

Control ![[bound]] binds as embed
